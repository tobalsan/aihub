# Chat Context Usage Visibility

- Adjusted `ChatView` context usage styling so the estimate is left-aligned under the composer, slightly larger, and higher contrast.

Verification:
- `pnpm exec vitest run apps/web/src/components/ChatView.test.tsx`
