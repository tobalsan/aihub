# Remove Project Recent Runs Bar

- Removed the project detail horizontal `Recent Runs` strip from `BoardProjectDetailPage`.
- Left the project `Agent` tab and slice-level Recent Runs UI unchanged.
- Verified `BoardProjectDetailPage.test.tsx` passes.
