# HTTP admin-guard probes (multiUser mode, no session)

## GET /api/agents/sales/extensions (unauthenticated)
HTTP 401
{"error":"unauthorized"}

## GET /api/pool (unauthenticated)
HTTP 401
{"error":"unauthorized"}

## GET /api/capabilities (public)
{"multiUser":true,"extensions":{"multiUser":true}}
