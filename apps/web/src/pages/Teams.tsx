export function Teams() {
  return (
    <div class="teams-page">
      <h1 class="teams-heading">Teams</h1>
      <p class="teams-subtext">Coming soon.</p>

      <style>{`
        .teams-page {
          padding: 24px;
        }

        .teams-heading {
          font-size: 24px;
          font-weight: 600;
          color: var(--text-primary);
          margin-bottom: 8px;
        }

        .teams-subtext {
          font-size: 14px;
          color: var(--text-tertiary);
        }
      `}</style>
    </div>
  );
}
