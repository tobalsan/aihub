FROM aihub-eval-base:local

# This task uses the base image as-is. Any task-specific files (e.g.,
# fixture overrides) would be COPY'd here. sales-admin-renewals uses
# the base fixtures from the fake-cloudifi-admin sidecar unchanged.
