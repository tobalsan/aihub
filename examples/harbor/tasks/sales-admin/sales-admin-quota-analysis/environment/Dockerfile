FROM aihub-eval-base:local

# This task uses the base image as-is. The quota_report.json and
# companies.json fixtures in the fake-cloudifi-admin sidecar provide
# all data needed for the quota analysis task.
