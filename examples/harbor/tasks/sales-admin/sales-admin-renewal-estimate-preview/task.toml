schema_version = "1.1"

[task]
name = "aihub/sales-admin-renewal-estimate-preview"
description = "Build a renewal estimate preview from company pricing and quota usage"
authors = [{ name = "AIHub evals", email = "evals@aihub.local" }]
keywords = ["sales-admin", "cloudifi-admin", "deterministic"]

[metadata]
wave = 0
category = "sales-admin"
aihub_agent = "sally"
difficulty_explanation = "Read-only deterministic pricing preview using company + quota fixtures"

[verifier]
timeout_sec = 120.0

[verifier.env]
EVAL_NOW = "2026-04-06"

[agent]
timeout_sec = 300.0
user = "agent"

[environment]
build_timeout_sec = 600.0
cpus = 1
memory_mb = 2048
storage_mb = 4096
allow_internet = true

[environment.env]
REQUESTY_API_KEY = "${REQUESTY_API_KEY}"
